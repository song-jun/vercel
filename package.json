{
  "name": "mr-song"，
  "version": "1.0.0"，
  "description": "君陌离"，
  "author": "MrSong"，
  "license": "MIT"，
  "scripts": {
    "sb": "yarn add pm2 -g"，
    "init": "node ./dbInit && set NODE_ENV=production && pm2 start ./index.js --watch"
  }，
  "keywords": [
    "君陌离"
  ]，
  "dependencies": {
    "axios": "^0.19.0"，
    "mysql": "^2.13.0"
  }，
  "devDependencies": {
    "multer": "^1.4.2",
    "express": "^4.18.2"
  }，
  "engines": {
    "node": ">=8.9"，
    "npm": ">= 3.0.0"
  }，
  "browserslist": [
    "> 1%"，
    "last 2 versions"
  ]
}
